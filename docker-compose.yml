version: '3.8'

services:
  patrimonix:
    build: .
    container_name: patrimonix
    ports:
      - '3000:3000'
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
